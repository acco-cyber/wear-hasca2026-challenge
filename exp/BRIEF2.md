# Update to BRIEF.md (2026-09-24) — read BRIEF.md first, then this.

- Public LB: our best **0.87061** ("e7"). Rank 3 now needs **0.88408**; #1 is 0.89847. We need +0.014.
- e7 recipe: base probs = log-blend 0.5*exp\base\v3b + 0.3*work\lgbm_v1 + 0.2*work\fusion_v1 (OOF file exp\base\bl_v3b_v1_f\oof.npy; test file work\probs_v3bv1f_votes.npy which ALSO contains +logit vote bonuses from three external submissions) -> exp\decoder\decoder.py `decode_subject(P, st, VARIANTS['mrf4'], None)` -> within-subject video-kNN label spreading (exp\transductive\refine_core.knn_label_Q on tlib.build_X(F,'v768',128,32), k5, alpha .9, eps .1, L += 6*log Q) -> mrf4 again.
- Sim harness for the e7 recipe on 18 sessions (eval sbj_0,5,10,14_2,20,21 / extra sbj_2,4,8,9,13,17 / extra2 sbj_0_2,6,11,14,15,18): `exp\pl\sim_final.py <oof.npy> "6,0"` -> e7 recipe = eval 0.8224 / extra 0.8637 / extra2 0.8622 / all18 0.8495. Structs: work\sim_struct.pkl (eval), exp\transductive\{extra,extra2}_struct.pkl; per-session features/labels cached in exp\transductive\cache_{eval,extra,extra2}.pkl (dict session -> {n, y, F, ...}). Loader: exp\pl\pl_labels.py `_init` / `_G`.
- LESSON: sim gains below ~+0.01 did NOT transfer to the leaderboard (two tweaks at +0.002/+0.005 sim lost ~0.004 on LB). Only report ideas with >= +0.01 all18 AND gains on each of the three session sets.
- ENVIRONMENT: pandas' compiled `indexing` module is now blocked by Windows application control. ALWAYS run python with `$env:PYTHONPATH = "E:\Claude code\wear\shim"` (a sitecustomize there provides a pure-Python replacement). Do not try to change any security settings.
- Same hard rules: never kill python by name; no Kaggle submissions/notebooks; don't edit src/, data/, exp/base, exp/decoder, exp/transductive, exp/pl (copy what you need); cap threads at 3; subagents cannot write .md report files — return the report as your final message.
